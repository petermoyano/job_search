APP_ENV=local
DATABASE_URL=postgresql+psycopg://job_radar:job_radar@localhost:5432/job_radar

# Optional. CV extraction can use deterministic parsing without this.
OPENAI_API_KEY=
LLM_MODEL=gpt-4.1-mini

